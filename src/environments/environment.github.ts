/** GitHub Pages build: same as prod but demo data/permissions so the app is usable without backend login. */
export const environment = {
  production: true,
  orgId: 80,
  orgName: 'OPL',
  platFormId: 1,
  staticDemo: true,
};
